import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "DoctorSmile.ca - Natural Veneers, Natural Smiles, Natural Transformations",
  description:
    "Transform your smile with natural-looking veneers. Virtual consultations available. Serving London and Oakville, Ontario.",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
